# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Tinkoff_chat' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Tinkoff_chat
  pod 'SwiftLint'

  target 'Tinkoff_chatTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Tinkoff_chatUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
